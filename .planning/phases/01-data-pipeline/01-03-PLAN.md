---
phase: 01-data-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "A known CVE with references (CVE-2021-44228) produces has_refs=true and a non-empty matrix in the prepare job"
    - "A CVE with no references exits cleanly without a workflow error and shows a summary message"
    - "An invalid CVE ID format causes the prepare job to fail immediately with a clear error annotation"
  artifacts:
    - path: ".github/workflows/archive-cve.yml"
      provides: "Complete Phase 1 prepare job"
      contains: "workflow_dispatch"
  key_links:
    - from: "workflow_dispatch inputs.cve_id"
      to: "prepare job outputs: matrix, has_refs, ref_count"
      via: "fetch → extract step"
      pattern: "steps.extract.outputs"
---

<objective>
Verify the complete Phase 1 prepare job works end-to-end against real CVE data.

Purpose: Phase 1's success criteria require that a real CVE ID produces the correct outputs. This checkpoint confirms all four requirements (PIPE-01 through PIPE-04) are satisfied before Phase 2 planning begins.
Output: Human-verified confirmation that the workflow behaves correctly for all three test cases.
</objective>

<execution_context>
@/Users/welch/Code/cve/refgha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/welch/Code/cve/refgha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-pipeline/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify prepare job against three test cases</name>
  <files>.github/workflows/archive-cve.yml</files>
  <action>
    Human verification checkpoint. No automated changes. Run the three test cases below in the GitHub Actions UI to confirm Phase 1 is complete.
  </action>
  <what-built>
    Complete Phase 1 prepare job in .github/workflows/archive-cve.yml that:
    - Accepts a CVE ID via workflow_dispatch (PIPE-01)
    - Fetches CVE JSON from GitHub raw content (PIPE-02)
    - Extracts reference URLs with null-safe jq (PIPE-03)
    - Handles zero-reference CVEs without a workflow error (PIPE-04)
  </what-built>
  <how-to-verify>
    Run three workflow tests from the GitHub UI (Actions tab → "Archive CVE References" → "Run workflow"):

    **Test 1 — Valid CVE with many references (PIPE-02, PIPE-03)**
    Input: `CVE-2021-44228`
    Expected: Prepare job succeeds. Check the job summary — should show "X reference(s) found" (expect 50+). Check job outputs in the GHA log for `has_refs=true` and a non-empty `matrix` JSON array containing objects with `index` and `url` fields.

    **Test 2 — CVE with zero or few references (PIPE-04)**
    Input: `CVE-1999-0001`
    Expected: Prepare job succeeds (green checkmark, NOT a workflow error). Job summary shows "No References Found" OR a small reference count. If has_refs=false: no matrix output, clean exit. If has_refs=true: small matrix. Either way, workflow run completes without error.

    **Test 3 — Invalid CVE ID format (format validation)**
    Input: `cve-2021-44228` (lowercase)
    Expected: Prepare job FAILS immediately with an error annotation: "Invalid CVE ID format". No curl fetch should occur. The error message should be visible in the job log.

    To inspect outputs: click the prepare job → expand "Fetch and extract CVE references" step → look for the `matrix=`, `has_refs=`, `ref_count=` lines in the log output.
  </how-to-verify>
  <verify>Human approval via resume-signal below.</verify>
  <done>All three test cases pass as described above.</done>
  <resume-signal>Type "approved" if all three tests pass as expected, or describe any failures for diagnosis.</resume-signal>
</task>

</tasks>

<verification>
Human verification via GitHub Actions UI as described above.
</verification>

<success_criteria>
- Test 1 (CVE-2021-44228): Prepare job green, summary shows 50+ references, matrix JSON contains {index, url} objects
- Test 2 (CVE-1999-0001): Prepare job green (no workflow-level error), summary message present
- Test 3 (lowercase cve id): Prepare job red with "Invalid CVE ID format" annotation, no fetch attempted
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-03-SUMMARY.md`
</output>
